# 本地开发配置文件 - 组员在各自电脑上运行
# 启动命令: docker-compose up -d

version: '3.8'

services:
  # 后端服务
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: mikmok-backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - SPRING_DATASOURCE_URL=jdbc:postgresql://${SERVER_IP}:5432/${DB_NAME}
      - SPRING_DATASOURCE_USERNAME=${DB_USER}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - SPRING_REDIS_HOST=${SERVER_IP}
      - SPRING_REDIS_PASSWORD=${REDIS_PASSWORD}
      - MINIO_ENDPOINT=http://${SERVER_IP}:9000
      - MINIO_ACCESS_KEY=${MINIO_ROOT_USER}
      - MINIO_SECRET_KEY=${MINIO_ROOT_PASSWORD}
    depends_on:
      - frontend # 确保网络连通，虽然基础设施在云端

  # 前端服务
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: mikmok-frontend
    ports:
      - "3000:3000"
    environment:
      - VITE_API_BASE_URL=http://localhost:8080/api
    stdin_open: true
    tty: true

  # Nginx 反向代理 (可选，用于模拟生产环境)
  nginx:
    image: nginx:alpine
    container_name: mikmok-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
    depends_on:
      - backend
      - frontend
